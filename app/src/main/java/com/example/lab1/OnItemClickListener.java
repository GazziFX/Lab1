package com.example.lab1;

public interface OnItemClickListener {
    /**
     * Обрабатывает нажатый элемент
     *
     * @param position позиция в списке
     */
    void onItemClick(int position);
}